import React from "react";
import Layout from "../components/Layout/Layout";

const Returns = () => (
  <Layout title="Returns & Exchanges">
    <div className="container py-5">
      <h1>Returns & Exchanges</h1>
      <p>
        This is a placeholder for the Returns & Exchanges page. Please update
        with your actual returns and exchanges policies.
      </p>
    </div>
  </Layout>
);

export default Returns;
